[package]
name = "server_role_example"
version = "0.25.5"
edition = "2021"
publish = false

[dependencies]
bevy = { workspace = true }
lightyear = { path = "../../lightyear", features = ["server", "client", "udp", "webtransport", "netcode"] }
clap = { version = "4.0", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }

[[bin]]
name = "server_role_example"
path = "src/main.rs"
